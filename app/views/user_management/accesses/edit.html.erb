<% title "Editar Acceso" %>

 

<%= links  link_to 'Configuraciones', [:configuration_management,:configurations] %> 
<% links  link_to 'Accesos', [:user_management,:accesses] %>   

<%= form_for [:user_management, @access],:html=>{:id=>"validate_form"} do |f| %>
<div class="well">
  <h2 class="semi-bold">Editar Acceso</h2>
  <%= flash_messages %>

  <%  @access.errors.each do |attr,message| %>
  <div class="alert alert-danger fade in">
    <button class="close" data-dismiss="alert">×</button>
    <i class="fa-fw fa fa-warning"></i> <%= message %>
  </div>
  <% end %>
    
  <fieldset>
 
      <p><label class="label label-info tas">Nombre</label> <%= f.text_field :name, class: 'form-control validate[required]'  %></p>
    
  </fieldset>
</div>

<div id="buttons" style="display:inline">
      <%= f.submit "Guardar", class: 'btn btn-gray text-white rounded-pill' %>
      <%= link_to 'Regresar', [:user_management,:accesses], class: "btn btn-gray text-white rounded-pill" %>
</div>

<% end %>
  <!-- begin layout--> 
<%= render :partial =>"layouts/app_bottom_script" %>
<%= render :partial =>"layouts/form_validation" %>
<!-- end layout--> 